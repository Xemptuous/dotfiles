# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)
    # enabled = no;

    # https://wiki.hyprland.org/Configuring/Animations/
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1
    bezier = snappy,0.2,0.6,0.4,1.0  # More snappy than easeOutQuint

    # Fastest
    # animation = specialWorkspace, 1, 4, default, fade
    # animation = border, 1, 10, default
    # animation = global, 1, 10, default
    # animation = windows, 0, 0.01, quick
    # animation = windowsIn, 1, 0.01, quick, popin 87%
    # animation = windowsOut, 1, 0.01, quick, popin 87%
    # animation = fadeIn, 1, 1.01, almostLinear
    # animation = fadeOut, 1, 1.01, almostLinear
    # animation = fade, 1, 1.01, quick
    # animation = layers, 1, 1.01, easeOutQuint
    # animation = layersIn, 1, 1.01, easeOutQuint, fade
    # animation = layersOut, 1, 1.01, linear, fade
    # animation = fadeLayersIn, 1, 1.01, almostLinear
    # animation = fadeLayersOut, 1, 1.01, almostLinear
    # animation = workspaces, 1, 1.01, almostLinear, fade
    # animation = workspacesIn, 1, 1.01, almostLinear, fade
    # animation = workspacesOut, 1, 1.01, almostLinear, fade

    # Faster
    bezier = linear,0,0,1,1
    bezier = quick,0.15,0,0.1,1
    bezier = snappy,0.2,0.6,0.4,1.0

    animation = specialWorkspace, 1, 2.5, quick, fade
    animation = border, 1, 6, quick
    animation = global, 1, 6, quick
    animation = windows, 1, 2.8, snappy
    animation = windowsIn, 1, 2.3, snappy, popin 87%
    animation = windowsOut, 1, 1.1, linear, popin 87%
    animation = fadeIn, 1, 1.2, quick
    animation = fadeOut, 1, 1.0, quick
    animation = fade, 1, 1.0, quick
    animation = layers, 1, 2.4, snappy
    animation = layersIn, 1, 2.2, snappy, fade
    animation = layersOut, 1, 1.0, linear, fade
    animation = fadeLayersIn, 1, 1.0, quick
    animation = fadeLayersOut, 1, 0.9, quick
    animation = workspaces, 1, 1.5, snappy, fade
    animation = workspacesIn, 1, 1.0, snappy, fade
    animation = workspacesOut, 1, 1.4, snappy, fade

    # # Default (full)
    # bezier = easeOutQuint,0.23,1,0.32,1
    # bezier = easeInOutCubic,0.65,0.05,0.36,1
    # bezier = linear,0,0,1,1
    # bezier = almostLinear,0.5,0.5,0.75,1.0
    # bezier = quick,0.15,0,0.1,1
    # bezier = snappy,0.2,0.6,0.4,1.0  # More snappy than easeOutQuint
    # animation = specialWorkspace, 1, 4, default, fade
    # animation = border, 1, 10, default
    # animation = global, 1, 10, default
    # animation = windows, 1, 4.79, easeOutQuint
    # animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    # animation = windowsOut, 1, 1.49, linear, popin 87%
    # animation = fadeIn, 1, 1.73, almostLinear
    # animation = fadeOut, 1, 1.46, almostLinear
    # animation = fade, 1, 3.03, quick
    # animation = layers, 1, 3.81, easeOutQuint
    # animation = layersIn, 1, 4, easeOutQuint, fade
    # animation = layersOut, 1, 1.5, linear, fade
    # animation = fadeLayersIn, 1, 1.79, almostLinear
    # animation = fadeLayersOut, 1, 1.39, almostLinear
    # animation = workspaces, 1, 1.94, almostLinear, fade
    # animation = workspacesIn, 1, 1.21, almostLinear, fade
    # animation = workspacesOut, 1, 1.94, almostLinear, fade
}
